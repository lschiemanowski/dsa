# Private Data Chat

Ask a quantitative question about the configured private database. The clarification model sees
only synthetic example data and helps make the question precise. You will see and approve the
exact request before DSA uses the real database with the separately configured trusted model.

After DSA finishes, the conversation stops. A verified derivation notebook is attached when one
was produced.
